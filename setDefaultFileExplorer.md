Query which is the default with:
```bash
xdg-mime query default inode/directory
```

After installing pantheon-files, set it as default with:
```bash
xdg-mime default io.elementary.files.desktop inode/directory
```
